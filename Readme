-Just run make on the main folder 
-You will be asked to give the name of the folder which contains the hash version you want to use (sha256 or sha3)
-After execution, you have to do "make clean" before testing another version of the hashcodes 
